/*
    Copyright (C) 2026 Fredrik Johansson

    This file is part of FLINT.

    FLINT is free software: you can redistribute it and/or modify it under
    the terms of the GNU Lesser General Public License (LGPL) as published
    by the Free Software Foundation; either version 3 of the License, or
    (at your option) any later version.  See <https://www.gnu.org/licenses/>.
*/

/* GENERATED by dev/tune_fixed.py (exp, n = 5, r = 16) -- do not
   edit directly; retune or re-emit with
       python3 dev/tune_fixed.py exp 5 --pin 16 --emit */

#include "flint.h"
#include "longlong.h"
#include "mpn_extras.h"
#include "fixed.h"
#include "impl.h"
#include "hand_mulhi.inc"

#if FLINT_BITS == 64

/* n = 5, r = 16: N = 17 terms; level widths V2:5 V3:5 V4:4 V5:4 V6:4 V7:4 V8:3 V9:3 V10:3 V11:3 V12:2 V13:2 V14:2 V15:2 V16:1 */
static void
_fixed_exp_series_5(nn_ptr res, nn_srcptr x)
{
    ulong v, w1, w0, h, l, cy;
    ulong A[5], B[5], T[5], x2[5];
    ulong X2 = x[4], X1 = x[3];

    (void) v; (void) w1; (void) w0; (void) h; (void) l;

    v = UWORD(0x00000000000d73f9);

    /* V_15 = C_15 + x V_16  (2 x 1) */
    _fixed_mulhi_2x1(&h, &l, X2, X1, v);
    add_ssaaaa(w1, w0, UWORD(0x0000000000d73f9f), UWORD(0x399dc0f88ec32b58), h, l);

    /* V_14 = C_14 + x V_15  (2 x 2) */
    _fixed_mulhi_2x2_sloppy(&h, &l, X2, X1, w1, w0);
    add_ssaaaa(w1, w0, UWORD(0x000000000c9cba54), UWORD(0x603e4e905d6f8a2e), h, l);

    /* V_13 = C_13 + x V_14  (2 x 2) */
    _fixed_mulhi_2x2_sloppy(&h, &l, X2, X1, w1, w0);
    add_ssaaaa(w1, w0, UWORD(0x00000000b092309d), UWORD(0x43684be51c198e91), h, l);

    /* V_12 = C_12 + x V_13  (2 x 2) */
    _fixed_mulhi_2x2_sloppy(&h, &l, X2, X1, w1, w0);
    add_ssaaaa(w1, w0, UWORD(0x00000008f76c77fc), UWORD(0x6c4bdaa26d4c3d67), h, l);

    /* V_11 = C_11 + x V_12  (3 x 2) */
    _fixed_mulhi_3x2(&A[2], &A[1], &A[0], x[4], x[3], x[2],
        w1, w0);
    B[0] = UWORD(0x71c7880adcbc46da);
    B[1] = UWORD(0x138e3f9d1f92e0df);
    B[2] = UWORD(0x0000006b99159fd5);
    mpn_add_n(A, A, B, 3);

    /* V_10 = C_10 + x V_11  (3 x 3) */
    flint_mpn_mulhigh_n(T, x + 2, A, 3);
    B[0] = UWORD(0xe392d8777c170b65);
    B[1] = UWORD(0xd71cbbc05b4fa999);
    B[2] = UWORD(0x0000049f93edde27);
    mpn_add_n(A, T, B, 3);

    /* V_9 = C_9 + x V_10  (3 x 3) */
    flint_mpn_mulhigh_n(T, x + 2, A, 3);
    B[0] = UWORD(0xe3bc74aad8e671f5);
    B[1] = UWORD(0x671f5583911ca002);
    B[2] = UWORD(0x00002e3bc74aad8e);
    mpn_add_n(A, T, B, 3);

    /* V_8 = C_8 + x V_9  (3 x 3) */
    flint_mpn_mulhigh_n(T, x + 2, A, 3);
    B[0] = UWORD(0x01a01a01a01a01a0);
    B[1] = UWORD(0xa01a01a01a01a01a);
    B[2] = UWORD(0x0001a01a01a01a01);
    mpn_add_n(A, T, B, 3);

    /* V_7 = C_7 + x V_8  (4 x 3) */
    B[0] = UWORD(0);
    B[1] = A[0];
    B[2] = A[1];
    B[3] = A[2];
    flint_mpn_mulhigh_n(T, x + 1, B, 4);
    B[0] = UWORD(0xd00d00d00d00d00d);
    B[1] = UWORD(0x0d00d00d00d00d00);
    B[2] = UWORD(0x00d00d00d00d00d0);
    B[3] = UWORD(0x000d00d00d00d00d);
    mpn_add_n(A, T, B, 4);

    /* V_6 = C_6 + x V_7  (4 x 4) */
    flint_mpn_mulhigh_n(T, x + 1, A, 4);
    B[0] = UWORD(0xb05b05b05b05b05b);
    B[1] = UWORD(0x5b05b05b05b05b05);
    B[2] = UWORD(0x05b05b05b05b05b0);
    B[3] = UWORD(0x005b05b05b05b05b);
    mpn_add_n(A, T, B, 4);

    /* V_5 = C_5 + x V_6  (4 x 4) */
    flint_mpn_mulhigh_n(T, x + 1, A, 4);
    B[0] = UWORD(0x2222222222222222);
    B[1] = UWORD(0x2222222222222222);
    B[2] = UWORD(0x2222222222222222);
    B[3] = UWORD(0x0222222222222222);
    mpn_add_n(A, T, B, 4);

    /* V_4 = C_4 + x V_5  (4 x 4) */
    flint_mpn_mulhigh_n(T, x + 1, A, 4);
    B[0] = UWORD(0xaaaaaaaaaaaaaaaa);
    B[1] = UWORD(0xaaaaaaaaaaaaaaaa);
    B[2] = UWORD(0xaaaaaaaaaaaaaaaa);
    B[3] = UWORD(0x0aaaaaaaaaaaaaaa);
    mpn_add_n(A, T, B, 4);

    /* V_3 = C_3 + x V_4  (5 x 4) */
    B[0] = UWORD(0);
    B[1] = A[0];
    B[2] = A[1];
    B[3] = A[2];
    B[4] = A[3];
    flint_mpn_mulhigh_n(T, x + 0, B, 5);
    B[0] = UWORD(0xaaaaaaaaaaaaaaaa);
    B[1] = UWORD(0xaaaaaaaaaaaaaaaa);
    B[2] = UWORD(0xaaaaaaaaaaaaaaaa);
    B[3] = UWORD(0xaaaaaaaaaaaaaaaa);
    B[4] = UWORD(0x2aaaaaaaaaaaaaaa);
    mpn_add_n(A, T, B, 5);

    /* V_2 = C_2 + x V_3  (5 x 5) */
    flint_mpn_mulhigh_n(T, x + 0, A, 5);
    B[0] = UWORD(0x0000000000000000);
    B[1] = UWORD(0x0000000000000000);
    B[2] = UWORD(0x0000000000000000);
    B[3] = UWORD(0x0000000000000000);
    B[4] = UWORD(0x8000000000000000);
    mpn_add_n(A, T, B, 5);

    /* x^2 V_2 */
    flint_mpn_sqrhigh(x2, x, 5);
    flint_mpn_mulhigh_n(T, x2, A, 5);
    cy = mpn_add_n(res, x, T, 5);
    res[5] = 1 + cy;
}

void
fixed_exp_opt_5(nn_ptr res, nn_srcptr x)
{
    const int r = 16;
    slong i, j, num, bj, nc;
    ulong t0, t1, t2, t3, t4, bw, h, e;
    ulong d0, d1, d2, d3, d4;
    ulong y0, y1, y2, y3, y4, y5;
    ulong s0, s1, s2, s3, s4, s5;
    ulong ts[5], ys[7], sh[6];
    slong used[5 * FLINT_BITS + 2];


    _fixed_exp_logs_ensure(5, r);
    nc = _fixed_exp_logs_n;

    t0 = x[0];
    t1 = x[1];
    t2 = x[2];
    t3 = x[3];
    t4 = x[4];

/* exact compare-subtract of L_ii, recording the index */
#define STEP(ii) \
    do { \
        nn_srcptr Lq = _fixed_exp_logs + (ii) * nc + (nc - 5); \
        sub_ddddddmmmmmmssssss(bw, d4, d3, d2, d1, d0, \
            UWORD(0), t4, t3, t2, t1, t0, \
            UWORD(0), Lq[4], Lq[3], Lq[2], Lq[1], Lq[0]); \
        t0 = (t0 & bw) | (d0 & ~bw); \
        t1 = (t1 & bw) | (d1 & ~bw); \
        t2 = (t2 & bw) | (d2 & ~bw); \
        t3 = (t3 & bw) | (d3 & ~bw); \
        t4 = (t4 & bw) | (d4 & ~bw); \
        used[num] = (ii); \
        num += (bw == 0); \
    } while (0)

/* apply the pending recorded subtractions */
#define FLUSH() \
    for (; bj < num; bj++) \
    { \
        nn_srcptr Lq = _fixed_exp_logs + used[bj] * nc + (nc - 5); \
        sub_dddddmmmmmsssss(t4, t3, t2, t1, t0, \
            t4, t3, t2, t1, t0, \
            Lq[4], Lq[3], Lq[2], Lq[1], Lq[0]); \
    }

    num = 0;
    bj = 0;

/* one reduction window: decisions on the single-limb model
   hreg, exact steps at the boundary and inside the ambiguity
   band (see _fixed_exp_reduce) */
#define WINDOW(cc, hreg) \
    do { \
        { \
            slong i1 = FLINT_MIN((slong) r, \
                FLINT_BITS * ((cc) + 1) - 1); \
            nn_srcptr lp; \
            FLUSH(); \
            STEP(FLINT_BITS * (cc)); \
            bj = num; \
            h = hreg; \
            e = 0; \
            lp = _fixed_exp_logs + (FLINT_BITS * (cc) + 1) * nc \
                + (nc - 1 - (cc)); \
            for (i = FLINT_BITS * (cc) + 1; i <= i1; i++, lp += nc) \
            { \
                ulong lt = lp[0]; \
                if (h - lt + e <= 2 * e) \
                { \
                    FLUSH(); \
                    STEP(i); \
                    bj = num; \
                    h = hreg; \
                    e = 0; \
                } \
                else \
                { \
                    ulong sub = (h > lt); \
                    h -= (lt + 1) & (-sub); \
                    used[num] = i; \
                    num += sub; \
                    e += sub; \
                } \
            } \
        } \
    } while (0)

    WINDOW(0, t4);

#undef WINDOW

    FLUSH();

    /* one extra subtraction restores t < L_r < 2^-r */
    STEP(r);

#undef STEP
#undef FLUSH

    /* exp of the reduced argument */
    ts[0] = t0;
    ts[1] = t1;
    ts[2] = t2;
    ts[3] = t3;
    ts[4] = t4;
    _fixed_exp_series_5(ys, ts);
    y0 = ys[0];
    y1 = ys[1];
    y2 = ys[2];
    y3 = ys[3];
    y4 = ys[4];
    y5 = ys[5];

    j = 0;

    if (num > 0 && used[0] == 0)
    {
        add_ssssssaaaaaaaaaaaa(y5, y4, y3, y2, y1, y0,
            y5, y4, y3, y2, y1, y0, y5, y4, y3, y2, y1, y0);
        j = 1;
    }

    for (; j < num && used[j] < FLINT_BITS; j++)
    {
        int b = (int) used[j];

        s0 = MPN_RIGHT_SHIFT_LOW(y1, y0, b);
        s1 = MPN_RIGHT_SHIFT_LOW(y2, y1, b);
        s2 = MPN_RIGHT_SHIFT_LOW(y3, y2, b);
        s3 = MPN_RIGHT_SHIFT_LOW(y4, y3, b);
        s4 = MPN_RIGHT_SHIFT_LOW(y5, y4, b);
        s5 = y5 >> b;
        add_ssssssaaaaaaaaaaaa(y5, y4, y3, y2, y1, y0,
            y5, y4, y3, y2, y1, y0, s5, s4, s3, s2, s1, s0);
    }

    ys[0] = y0;
    ys[1] = y1;
    ys[2] = y2;
    ys[3] = y3;
    ys[4] = y4;
    ys[5] = y5;

    /* rare factors with i >= FLINT_BITS */
    if (j < num)
        _fixed_exp_recon(ys, sh, 6, used, j, num);

    res[0] = ys[0];
    res[1] = ys[1];
    res[2] = ys[2];
    res[3] = ys[3];
    res[4] = ys[4];
    res[5] = ys[5];
}


#endif /* FLINT_BITS == 64 */
